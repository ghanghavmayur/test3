#name of the pipline
name: github action demo pipeline

on:
  push:
    branches:
      - main

jobs:
  demo-job:
    runs on: ubuntu-latest

    steps:
      - name: checkput code
        uses: actions/checkput@v2

      - name: print message
        runs: echo "welcome mayur"
